(function() {    if (define && require) {        define([], function() {            return CreateKObservableObject;        });    } else if (module) {        module.exports = CreateKObservableObject;    }    return CreateKObservableObject;}())
